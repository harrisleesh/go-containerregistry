name: Boilerplate

on:
  pull_request:
    branches: ['main']

permissions:
  contents: read

jobs:

  check:
    name: Boilerplate Check
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false # Keep running if one leg fails.
      matrix:
        extension:
        - go
        - sh

        # Map between extension and human-readable name.
        include:
        - extension: go
          language: Go
        - extension: sh
          language: Bash

    steps:
      - name: Check out code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: chainguard-dev/actions/boilerplate@142d22e4cec7c824db396585b9bc86f76f7c1732 # v1.6.35
        with:
          extension: ${{ matrix.extension }}
          language: ${{ matrix.language }}
